let n, i, j, p2, k;
let count1 = 0,
  count2 = 0;
console.log("enter the number:");
n = prompt();
for (i = 3; i < n; i++) {
  for (j = 2; j < i; j++) {
    if (i % j === 0) {
      count1++;
      break;
    }
  }
  p2 = i + 2;
  for (k = 2; k < i; k++) {
    if (p2 % k === 0) {
      count2++;
      break;
    }
  }
  if (count1 === 0 && count2 === 0) {
    console.log(i + 1);
  }
  count1 = 0;
  count2 = 0;
}
